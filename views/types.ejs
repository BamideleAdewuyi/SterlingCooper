<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <%- include("partials/header.ejs") %>
    <h1><%= title %></h1>
    <div class="types">
        <% for (const type of types) {%>
            <a href="types/<%= type.id %>"><%= type.type %></a>
        <% } %>
    </div>
</body>
</html>